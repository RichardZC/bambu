@model IEnumerable<Hra.Application.DTO.ListarMiembroDto>

<h1>Miembros</h1>


<div class="row mb-4">
    <div class="col-sm-6">
        <form asp-action="Listar">
            <input name="pBuscar" class="form-control" placeholder="Buscar por Apellidos luego <ENTER>" />
        </form>
    </div>
    <div class="col-sm-6">
        <div class="text-sm-end mt-2 mt-sm-0">            
            <a type="button" href="@Url.Action("Editar","Cliente", new { id=0  })" class="btn btn-primary">
                <i class="mdi mdi-plus-outline me-1"></i>Nuevo
            </a>
        </div>
    </div>
</div>

<table class="table table-hover table-bordered  mb-0">
    <thead class="table-light">
        <tr>
            <th>Grupo</th>
            <th>DNI</th>
            <th>
               Miembro
            </th>
            <th>
                Email
            </th>
            <th>
                Celular
            </th>
            <th>
                Estado
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Grupo
                </td>
                <td>
                    @item.Dni
                </td>
                <td>
                    @item.Miembro
                </td>

                <td>
                    @item.Correo
                </td>
                <td>
                    @item.Celular
                </td>
                <td>
                    @item.Estado
                </td>
                <td>
                    <a type="button" href="@Url.Action("Editar","Cliente", new { id=item.PersonaId  })" class="btn btn-primary btn-sm">Editar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
